import React from 'react';
import "./admin.scss"

interface Props {
}

const Admin = (props: Props) => {
    return (
        <div className="admin">
            
        </div>
    );
};

export default Admin;
